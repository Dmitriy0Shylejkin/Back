import { resolve } from 'path';

export default {
  'config': resolve('config', 'config.js'),
  'migrations': resolve('migrations'),
  'models': resolve('models'),
  'seeders': resolve('seeders'),
};
